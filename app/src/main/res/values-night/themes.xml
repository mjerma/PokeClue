<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PokeClue" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">?attr/colorSecondaryVariant</item>
        <item name="colorPrimaryVariant">@color/primary_night_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary_night</item>
        <item name="colorSecondaryVariant">@color/secondary_night_dark</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="SplashScreenTheme" parent="Theme.PokeClue">
        <item name="android:navigationBarColor">@color/black</item>
    </style>

    <style name="NavigationDrawer" parent="Theme.PokeClue">
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorControlHighlight">?attr/colorSecondary</item>
    </style>

    <style name="LayoutBackground" parent="Theme.PokeClue">
        <item name="colorPrimary">@android:color/black</item>
    </style>

    <style name="TextColor" parent="Theme.PokeClue">
        <item name="colorPrimary">@android:color/white</item>
    </style>

    <style name="SearchView" parent="Widget.AppCompat.SearchView">
        <!-- Background for the search query section (e.g. EditText) -->
        <item name="queryBackground">@color/lightBlack</item>
    </style>

</resources>