<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PokeClue" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="NavigationDrawer" parent="Theme.PokeClue">
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorControlHighlight">?attr/colorSecondary</item>
    </style>

    <style name="Theme.PokeClue.NoActionBar" parent="Theme.PokeClue">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="LayoutBackground" parent="Theme.PokeClue">
        <item name="colorPrimary">@android:color/white</item>
    </style>

    <style name="TextColor" parent="Theme.PokeClue">
        <item name="colorPrimary">@color/lightBlack</item>
    </style>

    <style name="SearchView" parent="Widget.AppCompat.SearchView">
        <!-- Background for the search query section (e.g. EditText) -->
        <item name="queryBackground">@color/white</item>
    </style>
</resources>